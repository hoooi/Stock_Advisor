# ============================================================
# requirements.txt
# 설치: pip install -r requirements.txt
# ============================================================

# 주식 데이터 (현재 사용)
pykrx>=1.0.47
finance-datareader>=0.9.0

# 데이터 처리 (Python 3.14 호환 버전)
pandas>=2.0.0
numpy>=2.0.0

# 웹 대시보드
streamlit>=1.32.0
plotly>=5.19.0

# HTTP 요청 (KIS API 전환 시 필요)
requests>=2.31.0

# ============================================================
# KIS API 전환 시 추가 설치 불필요
# requests는 이미 포함되어 있음
# ============================================================
